#!/usr/bin/env bash
# Pitwall installer (M0 stub).
# M6 will: build release binary, install to ~/.local/bin, install the
# systemd user unit, copy plugin/dev.pitwall to ~/.config/omarchy/plugins,
# and print the `omarchy plugin enable` instructions.
set -euo pipefail

echo "pitwall installer: not implemented yet (arrives with M6 packaging)."
echo "See ROADMAP.md."
exit 1
