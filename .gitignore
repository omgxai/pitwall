# Generated files
/target/
# NOTE: Cargo.lock is intentionally TRACKED (binary crate → reproducible builds).

# Editor / OS noise
.vscode/
.idea/
*.swp
*~
.DS_Store

# Local Pitwall runtime data (never commit live state or secrets)
*.db
*.db-journal
*.db-wal
*.db-shm
*.corrupt-*
state.json
state.tmp
.pitwall/local/

# Secrets (never commit API keys, tokens, or keyrings)
.env
.env.*
*.pem
*.key
config/*.toml
!config/*.example.toml

# Omarchy plugin build artifacts
plugin/**/*.zip

# Kiro spec workspace (local only, never committed)
.kiro/
